import {
  url,
  projectId,
  apiKey,
  databaseId,
  storageId,
  patientCollectionId,
  doctorCollectionId,
  appointmentCollectionId,
} from "@/appwrite/env";

const appwriteConfig = {
  url,
  projectId,
  apiKey,
  databaseId,
  storageId,
  patientCollectionId,
  doctorCollectionId,
  appointmentCollectionId,
};

export default appwriteConfig;
